<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="a_wasdk_app.DevicesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:a_wasdk_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ImageGridView_DefaultItemTemplate"
                      x:DataType="local:PhoneDevice">
            <Grid>
                <TextBlock Text="This is sample device" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="myButton" Click="myButton_Click">Start Devices Scan</Button>
        </StackPanel>


        <GridView x:Name="ImageGridView"
                Margin="0,0,0,8"
                ItemClick="ImageGridView_ItemClick"
                IsItemClickEnabled="True"
                ItemsSource="{x:Bind Devices, Mode=OneWay}"
                ItemTemplate="{StaticResource ImageGridView_DefaultItemTemplate}">
        </GridView>
    </StackPanel>
</Page>
